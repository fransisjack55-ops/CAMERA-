#ifndef SHARED_FRAME_H\n#define SHARED_FRAME_H\n\n#include <stdint.h>\n\n// Define the magic value and version number\n#define MAGIC_VALUE 0x53434231 // SCB1\n#define VERSION 1\n\n// SharedFrameHeader struct definition for NV12 frame data\ntypedef struct {\n    uint32_t magic; // Magic value\n    uint32_t version; // Version of the struct\n    uint32_t width; // Width of the frame\n    uint32_t height; // Height of the frame\n    uint32_t max_payload_size; // Maximum payload size\n} SharedFrameHeader;\n\n// Initialize the SharedFrameHeader for NV12 1280x720 resolution\nSharedFrameHeader initSharedFrameHeader() {\n    SharedFrameHeader header;\n    header.magic = MAGIC_VALUE;\n    header.version = VERSION;\n    header.width = 1280;\n    header.height = 720;\n    header.max_payload_size = (1280 * 720 * 3) / 2; // NV12 payload size\n    return header;\n}\n\n#endif // SHARED_FRAME_H